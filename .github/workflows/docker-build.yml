name: Build and Push Cordia Beacon

on:
  push:
    branches: [ main, master ]
    paths:
      - 'beacon-server/**'
      - '.github/workflows/docker-build.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'beacon-server/**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-beacon

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            beacon-server/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build Rust binary (postgres + redis enabled)
        working-directory: ./beacon-server
        run: cargo build --release --features "postgres,redis-backend"

      - name: Verify binary exists
        run: |
          echo "Checking for binary and entrypoint..."
          ls -la beacon-server/target/release/
          test -f beacon-server/target/release/cordia-beacon
          test -f beacon-server/entrypoint.sh
          echo "All required files present!"
          echo "Binary size:"
          du -h beacon-server/target/release/cordia-beacon

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build Docker image
        run: |
          echo "Building Docker image from repository root with context '.'"
          echo "Using Dockerfile: beacon-server/Dockerfile.prebuilt"
          docker build -f beacon-server/Dockerfile.prebuilt -t temp-build . || {
            echo "Docker build failed!"
            exit 1
          }
          echo "Docker build succeeded!"
          docker images temp-build

      - name: Tag and push images
        if: github.event_name != 'pull_request'
        run: |
          # Parse tags from metadata output (newline-separated)
          TAGS="${{ steps.meta.outputs.tags }}"
          echo "Tags to push:"
          echo "$TAGS"

          # Tag and push each one
          while IFS= read -r tag; do
            if [ -n "$tag" ]; then
              echo "Tagging and pushing: $tag"
              docker tag temp-build "$tag"
              docker push "$tag"
            fi
          done <<< "$TAGS"
